$("#item-<%= @menu_item.id %>").html("<%= j (render partial: 'menu_items/menuItems-list', locals: { menus: Menu.order(:name), order: @order }) %>").hide().fadeIn(300);
$(".all-messages").html("<%= j (render partial: 'home/messages', locals: {notice: "#{@order_item.menu_item_name} is added to cart!" }) %>")
